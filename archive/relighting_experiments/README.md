# Archived relighting experiments

These files preserve pre-V8 research history. They are not imported by the API,
worker, automatic pipeline, or canonical CLI.

Production relighting lives in:

```text
app/src/relighting/engine.py
```

The supported CLI wrapper is:

```text
app/scripts/relight_image.py
```
